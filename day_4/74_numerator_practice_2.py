# Creates a list made up of the tuples (index, element), formed from obtaining the indices of each character of the string "Python" by means of enumerate().

# Calls the list obtained with the variable name index_list .

string= "Python"


index_list = list(enumerate(string))
print(index_list)