#Use the random library's choice() method to get a random element from the list of names below, and store the chosen name in a variable called draw.

#names = ["Carlos", "Julia", "Nicole", "Laura", "Mailen"]

import random
names = ["Carlos", "Julia", "Nicole", "Laura", "Mailen"]
draw= random.choice(names)
print(draw)