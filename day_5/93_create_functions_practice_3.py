# Declare a function called square, which takes any number as an argument, and each time it is called, prints the square of that number (ie, the power 2 of the value) on the screen.

# The name of the argument that this function should take is a_number. Create this variable and assign it any number.

# You just have to define the function and create the variable, you don't have to call the function afterwards.

def square(a_number):
    print(a_number**2)

square(3)