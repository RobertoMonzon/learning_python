# Stores in the variable base_path, a Path object that points to the user's home directory.

# Remember to import Path from the pathlib module, and use the home() method

from pathlib import Path
 
ruta_base = Path.home()